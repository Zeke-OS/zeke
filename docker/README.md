Zeke in Docker
==============

This Docker image is used to build and test Zeke images.

Installation and Usage
----------------------

```
docker build -t zekedock .
docker run -it zekedock /bin/bash
./run-latest-zeke
```
